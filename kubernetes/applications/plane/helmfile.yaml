repositories:
  - name: makeplane
    url: https://helm.plane.so/

releases:
  - name: plane
    namespace: homelab
    createNamespace: true
    chart: makeplane/plane-ce
    version: 1.0.15
    values:
      - values.yaml
