repositories:
  - name: makeplane
    url: https://helm.plane.so/

releases:
  - name: plane
    namespace: homelab
    createNamespace: true
    chart: makeplane/plane-ce
    version: 1.0.12
    values:
      - values.yaml
