type: postgresql
mode: standalone

version:
  postgresql: "17"

cluster:
  instances: 3
  resources:
    limits:
      memory: 2Gi
      cpu: 2000m
    requests:
      cpu: 1000m
      memory: 1Gi
  postgresql:
    parameters:
      log_hostname: "off"
      log_timezone: "UTC"
      max_connections: "200"
      shared_buffers: "256MB"

backups:
  enabled: false

poolers:
  - name: pgbouncer
    type: rw
    instances: 1
    parameters:
      max_client_conn: "1000"
      default_pool_size: "300"
    template:
      spec:
        containers:
          - name: pgbouncer
            image: cloudnative-pg/pg-bouncer:1.23.0
            resources:
              limits:
                memory: 500Mi
              requests:
                cpu: 100m
                memory: 100Mi
        initContainers:
          - name: bootstrap-controller
            resources:
              limits:
                memory: 500Mi
              requests:
                cpu: 100m
                memory: 100Mi
